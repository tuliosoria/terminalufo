# Terminal UFO

A minimal Next.js Hello World app, configured for static export and AWS Amplify Hosting.

## Scripts

- `npm run dev` - start the local Next.js dev server
- `npm run build` - build the static export into `out/`
- `npm test` - run the Node.js scaffold regression test
